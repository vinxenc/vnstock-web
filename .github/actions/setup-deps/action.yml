name: Setup dependencies
description: Install pnpm + Node and restore (or install) node_modules from cache.
runs:
  using: composite
  steps:
    - name: Install pnpm
      uses: pnpm/action-setup@v4
      # No `version:` input — reads packageManager: pnpm@9.7.0 from package.json
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        # Node 24.x (Active LTS). Hard floor is 20.12 regardless — vitest 4's
        # rolldown imports util.styleText, absent on older Node — but the
        # project targets the 24 line.
        node-version: "24.x"
    - name: Restore node_modules cache
      id: cache
      uses: actions/cache@v4
      with:
        path: node_modules
        key: ${{ runner.os }}-node-modules-${{ hashFiles('pnpm-lock.yaml') }}
    - name: Install dependencies (cache miss only)
      if: steps.cache.outputs.cache-hit != 'true'
      shell: bash
      run: pnpm install --frozen-lockfile
